# PR-B 二段アセンブル E2E テスト用の最小 fixture environment。
# BEEP に @resident shared を付与して、RESIDENT モードの overlay から main resident
# な BEEP を CALL する経路をテストする。
env_type: 0
os_type: 0
default_org: "$100"
libraries:
  - twostage_runtime.yml
