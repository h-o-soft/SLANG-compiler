
PSG_BASE:
  lib_name: PSGLIB
  extlib: psgdriverx1.asm:PSG_COMMON

PSG_INIT:
  calls:
    - PSG_BASE
  lib_name: PSGLIB
  extlib: psgdriverx1.asm:PSG_INIT

PSG_PLAY:
  calls:
    - PSG_BASE
  lib_name: PSGLIB
  extlib: psgdriverx1.asm:PSG_PLAY

PSG_SFX:
  calls:
    - PSG_BASE
  lib_name: PSGLIB
  extlib: psgdriverx1.asm:PSG_SFX

PSG_STOP:
  calls:
    - PSG_BASE
  lib_name: PSGLIB
  extlib: psgdriverx1.asm:PSG_STOP

PSG_PAUSE:
  calls:
    - PSG_BASE
  lib_name: PSGLIB
  extlib: psgdriverx1.asm:PSG_PAUSE

PSG_RESUME:
  calls:
    - PSG_BASE
  lib_name: PSGLIB
  extlib: psgdriverx1.asm:PSG_RESUME

PSG_PROC:
  calls:
    - PSG_BASE
  lib_name: PSGLIB
  extlib: psgdriverx1.asm:PSG_PROC

VSYNC:
  code: |
    ; VSYNC
    LD BC,$1a01
    .LP1
    DB $ED,$70  ; IN F,(C)
    JP P,.LP1
    DI
    .LP2
    DB $ED,$70
    JP M,.LP2
    EI
    RET

